{
  "name": "swagcoders",
  "version": "1.0.0",
  "description": "World's largest coding community",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b shared && tsc -b backend && tsc -b frontend",
    "start": "npm run build && node backend/dist/app.js",
    "server": "cd backend && tsc -b && node dist/backend/src/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.7",
    "@types/jsonwebtoken": "^9.0.7"
  }
}
